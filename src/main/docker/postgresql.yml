version: '2'
services:
  abcimeditor-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/abCimEditor/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=abCimEditor
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
